name: Charts Release

on:
  push:
    branches:
      - main
    paths:
      - .github/pages/config.yml
      - .github/pages/index.md.hbs
      - application/**
      - library/**
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  release:
    name: Package and Publish
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure repository
        uses: actions/github-script@v7
        with:
          script: |
            const configureGit = require('./.github/scripts/git-config.js');
            await configureGit({github, context, core, exec});

      - name: Setup helm
        uses: azure/setup-helm@v4
        with:
          version: v3.17.3

      - name: Setup node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install node.js dependencies
        run: npm install js-yaml handlebars

      - name: Get chart-releaser configuration
        id: chart-config
        uses: actions/github-script@v7
        with:
          script: |
            // Load the module
            const pagesModule = require('./.github/scripts/pages.js');
            const { CONFIG, setOutputs } = pagesModule;
            
            // Set all outputs for later steps
            setOutputs(core);

            // Create dist directory
            const fs = require('fs/promises');
            try {
              await fs.mkdir(CONFIG.paths.distDir, { recursive: true });
              core.info(`Created directory: ${CONFIG.paths.distDir}`);
            } catch (error) {
              core.setFailed(`Error creating directory: ${error.message}`);
            }

            // Verify release template exists
            try {
              await fs.access(CONFIG.paths.releaseTemplate);
              core.info(`Release template found at: ${CONFIG.paths.releaseTemplate}`);
            } catch (error) {
              core.setFailed(`Release template not found at ${CONFIG.paths.releaseTemplate}: ${error.message}`);
            }

      - name: Release application charts
        uses: helm/chart-releaser-action@v1
        env:
          CR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CR_INDEX_PATH: ${{ steps.chart-config.outputs.CR_INDEX_PATH }}
          CR_SKIP_EXISTING: ${{ steps.chart-config.outputs.CR_SKIP_EXISTING }}
          CR_PAGES_BRANCH: ${{ steps.chart-config.outputs.CR_PAGES_BRANCH }}
          CR_CHARTS_REPO_URL: ${{ steps.chart-config.outputs.CR_CHARTS_REPO_URL }}
          CR_RELEASE_NAME_TEMPLATE: ${{ steps.chart-config.outputs.CR_RELEASE_NAME_TEMPLATE }}
          CR_RELEASE_TEMPLATE: ${{ steps.chart-config.outputs.CR_RELEASE_TEMPLATE }}
          CR_PACKAGES_WITH_INDEX: ${{ steps.chart-config.outputs.CR_PACKAGES_WITH_INDEX }}
        with:
          charts_dir: ${{ steps.chart-config.outputs.DIRECTORY_APPLICATION }}
          skip_existing: true

      - name: Release library charts
        uses: helm/chart-releaser-action@v1
        env:
          CR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CR_INDEX_PATH: ${{ steps.chart-config.outputs.CR_INDEX_PATH }}
          CR_SKIP_EXISTING: ${{ steps.chart-config.outputs.CR_SKIP_EXISTING }}
          CR_PAGES_BRANCH: ${{ steps.chart-config.outputs.CR_PAGES_BRANCH }}
          CR_CHARTS_REPO_URL: ${{ steps.chart-config.outputs.CR_CHARTS_REPO_URL }}
          CR_RELEASE_NAME_TEMPLATE: ${{ steps.chart-config.outputs.CR_RELEASE_NAME_TEMPLATE }}
          CR_RELEASE_TEMPLATE: ${{ steps.chart-config.outputs.CR_RELEASE_TEMPLATE }}
          CR_PACKAGES_WITH_INDEX: ${{ steps.chart-config.outputs.CR_PACKAGES_WITH_INDEX }}
        with:
          charts_dir: ${{ steps.chart-config.outputs.DIRECTORY_LIBRARY }}
          skip_existing: true

      - name: Generate chart index page
        id: generate_index
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs/promises');
            const pagesScriptPath = '${{ steps.chart-config.outputs.PAGES_SCRIPT_PATH }}';
            const { generateChartIndex } = require(pagesScriptPath);
            await generateChartIndex({ context, core, fs });

      - name: Setup build environment
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs/promises');
            const pagesScriptPath = '${{ steps.chart-config.outputs.PAGES_SCRIPT_PATH }}';
            const { setupBuildEnvironment } = require(pagesScriptPath);
            await setupBuildEnvironment({ core, fs });

      - name: Build website
        uses: actions/jekyll-build-pages@v1
        with:
          source: ./
          destination: ./_site

      - name: Prepare site for GitHub Pages
        uses: actions/github-script@v7
        with:
          script: |
            const pagesScriptPath = '${{ steps.chart-config.outputs.PAGES_SCRIPT_PATH }}';
            const { prepareSite } = require(pagesScriptPath);
            await prepareSite({ core });

      - name: Setup Pages
        id: pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./_site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
